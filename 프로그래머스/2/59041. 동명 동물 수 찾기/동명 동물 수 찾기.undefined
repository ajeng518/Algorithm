SELECT NAME,
        COUNT(NAME) AS COUNT
FROM ANIMAL_INS
GROUP BY NAME
HAVING COUNT(NAME)>1 AND NAME IS NOT NULL
ORDER BY 1
;













# SELECT NAME, COUNT(NAME) AS COUNT
# FROM ANIMAL_INS
# GROUP BY NAME
# HAVING COUNT(NAME)>1
# ORDER BY NAME
# ;